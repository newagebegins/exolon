define(function () {
  
  var baseID = 100;
  
  var screens = {
    LOADING: baseID,
    TITLE: baseID + 1,
    PLAY: baseID + 2,
    BONUS: baseID + 3,
  };
  
  return screens;
  
});
