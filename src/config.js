define(function () {
  
  var config = {
    maxLives: 9,
    
    initialAmmo: 99,
    initialGrenades: 10,
    initialPoints: 0,
    initialLives: 9,
    initialZones: 0,
    
    initialLevel: "L01S01",
    
     // "vitorc" or "exolon"
    initialVitorcOutfit: "vitorc",
    
    // debug
    renderHitBox: false,
    renderCollisionMap: false,
  };
  
  return config;
  
});
