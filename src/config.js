define(function () {
  
  var config = {
    initialAmmo: 99,
    initialGrenades: 10,
    initialPoints: 0,
    initialLives: 9,
    initialZones: 0,
    
    initialScreen: "L01S01",
    
    // debug
    renderHitBox: false,
    renderCollisionMap: false,
  };
  
  return config;
  
});
